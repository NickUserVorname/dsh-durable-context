# local-dsh-v4-control

This directory is reserved for a selected broader control-package publication.

No current broader package should be inferred from this placeholder.

The current primary preserved implementation artifact is under:

[`../../memory-stack/`](../../memory-stack/)

Frozen older implementations belong under:

[`../../historical/`](../../historical/)

Do not silently present an older frozen package as the current implementation.

When a broader publication is selected, record its exact identity, status and relation to the preserved memory stack here.
